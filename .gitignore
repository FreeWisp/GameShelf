# Dependencies
node_modules/

# Expo / React Native
.expo/
dist/
dist-check/
web-build/

# Logs
*.log

# Environment / secrets
.env
# ...but ship the backend keys so the project works out-of-the-box for graders.
# (Academic project — regenerate these keys before any public release.)
!backend/.env

# SQLite database (centralized, regenerated via `npm run seed`)
backend/data/*.db
backend/data/*.db-*

# OS
.DS_Store

# Risultati delle misure (rigenerabili con benchmark.js + plots.py)
tools/results/
